import [test item] from './../src/[test-item].js'

describe('[test-item]', () => {
  test('should [do a thing stated here]', () => {
    //code
    const date = new Date('2022-10-01');
    //expect statements
    expect([thing]).toEqual([other things]);
    expect(date.month).toEqual("10");
    expect(date.day).toEqual("01");
  });
});